import type { DotColor } from '../types/arrangement';

export const DOT_COLORS: Record<DotColor, string> = {
  red: '#E74C3C',
  blue: '#3498DB',
};

export const DOT_COLOR_LIST: DotColor[] = ['red', 'blue'];
